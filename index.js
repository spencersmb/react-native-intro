import { AppRegistry } from 'react-native'
import React from 'react'
import App from './App'

// Render to the device
// must register at least one component to the application
// the string must match the name of the project
AppRegistry.registerComponent('intro', () => App)
